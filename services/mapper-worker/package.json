{
  "name": "@dom/mapper-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Independent photogrammetry processing worker for the DOM Mapper module. Runs outside Vercel/Next.js — a long-lived Node process on a local DOM processing workstation that talks to NodeODM.",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "adm-zip": "^0.5.16",
    "exifr": "^7.1.3"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.5",
    "@types/node": "^22.7.5",
    "tsx": "^4.23.1",
    "typescript": "^5.6.3"
  }
}
