import DominicApp from "@/components/dominic/DominicApp";

export const metadata = {
  title: "DOMINIC | Intelligent Mapping by DOM",
  description: "Map. Measure. Analyze. Deliver.",
};

export default function DominicPage() {
  return <DominicApp />;
}
