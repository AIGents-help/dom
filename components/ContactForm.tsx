"use client";
import { FormEvent, useState } from "react";
import { CheckCircle2, Loader2 } from "lucide-react";
export default function ContactForm(){const[loading,setLoading]=useState(false),[success,setSuccess]=useState(false),[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const form=e.currentTarget;try{const r=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form).entries()))}),out=await r.json();if(!r.ok)throw new Error(out.error);form.reset();setSuccess(true)}catch(x:any){setError(x.message??"Your message could not be sent.")}finally{setLoading(false)}}if(success)return <div className="card p-10 text-center"><CheckCircle2 className="mx-auto h-12 w-12 text-accent"/><h2 className="mt-4 text-2xl font-black text-white">Message received.</h2><p className="mt-2 text-slate-400">Your message is in the DOM operations inbox. We’ll respond as soon as possible.</p><button className="btn-secondary mt-6" onClick={()=>setSuccess(false)}>Send another message</button></div>;return <form onSubmit={submit} className="card grid gap-5 p-7 sm:grid-cols-2 lg:p-9"><input name="website" tabIndex={-1} autoComplete="off" aria-hidden="true" className="hidden"/><Field label="Name *"><input className="input" name="name" required maxLength={120}/></Field><Field label="Email *"><input className="input" name="email" type="email" required maxLength={320}/></Field><Field label="Phone"><input className="input" name="phone" maxLength={60}/></Field><Field label="Company / Organization"><input className="input" name="company" maxLength={160}/></Field><Field label="Message type *"><select className="input" name="category" defaultValue="general_inquiry"><option value="general_inquiry">General inquiry</option><option value="partnership_opportunity">Partnership</option><option value="thank_you_feedback">Compliment / Thank you</option><option value="website_feedback">Website feedback</option><option value="billing">Billing question</option><option value="other">Other</option></select></Field><Field label="Subject *"><input className="input" name="subject" required maxLength={180}/></Field><div className="sm:col-span-2"><label className="label">Message *</label><textarea className="input min-h-40 resize-y" name="message" required maxLength={10000}/></div>{error&&<p className="text-sm text-red-400 sm:col-span-2">{error}</p>}<div className="sm:col-span-2"><button className="btn-primary" disabled={loading}>{loading?<><Loader2 className="h-4 w-4 animate-spin"/>Sending…</>:"Send Message"}</button></div></form>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <div><label className="label">{label}</label>{children}</div>}
